{
	"name": "paper-trail-api",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"types": "wrangler types",
		"seed": "wrangler d1 execute paper-trail-db --file=db/seed.sql",
		"seed:local": "wrangler d1 execute paper-trail-db --local --file=db/seed.sql"
	},
	"dependencies": {
		"hono": "^4.12.7",
		"stripe": "^20.4.1"
	},
	"devDependencies": {
		"wrangler": "^4.73.0"
	}
}
